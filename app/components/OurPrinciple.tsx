export default function OurPrinciple() {
  return (
    <section
      id="unser-grundsatz"
      className="text-center py-32.5 bg-[--background]"
    >
      <div className="max-w-310 mx-auto px-5 sm:px-10">
        <span className="inline-block text-[13px] font-bold uppercase tracking-[.14em] text-(--accent) mb-5">
          // Unser Grundsatz
        </span>
        <h2 className="text-(--primary) text-(length:--fs-display) font-medium leading-[1.22] tracking-[-.01em]">
          Die meisten Probleme sind nicht komplex.
          <br />
          <b className="font-bold text-(--primary)">Sie sind unklar.</b>
        </h2>
      </div>
    </section>
  );
}
